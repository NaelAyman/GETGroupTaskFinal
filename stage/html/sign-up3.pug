extends index
block main-content
  .login-page
    .container
      form.form.sign-level(action="index-login.html")
        h2(class="wow fadeInDown") تسجيل جديد
        h4(class="wow fadeInUp") بيانات شخصية
        .form-group(class="wow fadeInUp" data-wow-delay="0.2s")
          select(id="categories")
            option(value="" disabled) فئات الكتب
            option(value="") فلسفة
            option(value="") كيمياء
            option(value="") فيزياء
          label(for="categories") فئات الكتب
        .form-group(class="wow fadeInUp" data-wow-delay="0.4s")
          input.form-control(type="text" id="userName")
          label(for="userName") إسم المستخدم
        .form-group(class="wow fadeInUp" data-wow-delay="0.6s")
          input.form-control(type="password" id="password")
          label(for="password") كلمة المرور
        .form-group(class="wow fadeInUp" data-wow-delay="0.8s")
          input.form-control(type="password" id="confirmPassword")
          label(for="confirmPassword") تأكيد كلمة المرور
        .flex-btn(class="wow fadeInUp" data-wow-delay="1s")
          button.site-btn.more تسجيل
            i.fas.fa-chevron-left